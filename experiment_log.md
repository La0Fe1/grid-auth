# 实验日志

> 追加写入：时间戳、当前状态、观测到的数值、下一步决策（AGENTS.md 2.3）。

## 2026-09-13 项目启动
- 状态：仓库迁移完成——项目已从父仓库 `PycharmProjects\PythonProject` 移出至独立根目录 `PycharmProjects\project-grid-gnn`，独立 git 仓库已初始化（初始提交 chore: initialize autonomous research repository）。
- 观测：无实验运行（本日志记录的是启动动作，非实验数据）。
- 下一步：执行选题门控（方向 B 查重 + 创新性声明），通过后填写 experiment_plan.md，再进入代码重组与实验。

## 2026-09-13 选题门控通过
- 状态：方向 B 查重完成（文献逐篇经 Semantic Scholar/DOAJ/OpenAIRE API 核实）。三柱合一直接相同者 0 篇；高度相似 2 篇（Zhang et al. Applied Energy 2026；Fabrizio et al. arXiv:2509.02861）。原"GNN 策略"主创新表述否决（≥3 篇相似），新主创新=三柱组合。创新性声明见 paper/novelty_statement.md，核对表见 paper/literature_notes.md。
- 观测：TechRxiv 一篇候选的 DOI 在 Crossref 查无记录 → 标记未验证、禁止引用。
- 下一步：撰写 experiment_plan.md → 代码重组（src/ 结构 + 单元测试 + 冒烟测试）→ exp_001 环境验证。

## 2026-09-13 代码重组完成（任务 4 结束）
- 状态：src/ 六个模块全部重建并测试——graph/env/gnn_policy/n1_gate/shield/evaluation(评估+解释)/baselines(do-nothing/N1贪心/MLP)+ 训练三脚本。**51 项单元测试全部通过**；三个训练脚本 + MLP 基线冒烟测试全部跑通。
- 观测（冒烟实测）：GNN 策略 14,018 参数（远低于 100M 上限）；训练 FPS≈17（GNN）/29（MLP）；2048 步 125s。
- 观测（grid2op 源码+实测）：change_line_status 语义 = toggle，非法动作由环境拒绝；obs.simulate 返回 (obs, reward, done, info) 四元组。
- 下一步：exp_001（do-nothing/N1 贪心基线评估，与遗留 1052±689 对照）→ 全量 N-1 门标签生成与训练 → exp_002 主实验（300k 步 ×5 种子，≥4h/次）。

## 2026-09-13 exp_001 完成 + exp_002/003 启动
- 状态（exp_001）：do-nothing 评估 1100.7±282.8（5 种子×4 场景，与遗留 1052±689 同量级）；评估管线确定性验证通过（同场景同种子逐位一致）。
- 状态（exp_003 数据）：N-1 门标签正样本率 0.41（单层组合仿真修复嵌套 simulate bug 后）；门 val AUC=0.746、val acc=0.656、val bal=0.604。
- 状态（exp_002 启动）：GNN 种子 42/43/44 + MLP 种子 42，各 300k 步（≈4-5h），日志在 experiments/exp_002/logs/，checkpoint 每 10 分钟 + EvalCallback 周期评估。
- 失败记录（已解决，非 F1-F5 判据）：① 首版"完美 N-1"标签全零 → 谓词修订为可操作安全准则；② 嵌套 simulate 在 grid2op 1.12.5 不可靠（n1 恒 0）→ 改为单层组合仿真。两条均已写入 decisions.md。
- 下一步：改进版 N-1 贪心专家评估（后台）；36/118-bus 环境构造调查（exp_005）；训练期间准备 exp_003/004 脚本。

## 2026-09-13 N-1 贪心专家评估（exp_001 补充）
- 数值：贪心专家 5 种子存活 209/116/120/5/282（均值≈146，do-nothing≈1100），每幕仅 toggle 2-3 次，过载步数 0。
- 解读：朴素 N-1 贪心（仿真前瞻选"安全度改善最大"的 toggle）显著伤害存活——仿真前瞻未计入对手攻击与时序演化。这是诚实且有价值的发现：支持本文"屏蔽只删不安全动作、不强迫 toggle"的设计哲学；该基线在论文中如实报告（N-1 指标好、存活差）。
- 环境调查：36-bus=icaps_2021、118-bus=idf_2023 以内置开发模式（test=True）可用（118 变电站/186 线、36/59）；完整数据集下载源待查（Grid2Op releases）。
- 实测吞吐（4 进程并行 16 核）：单进程有效 ~40fps（GNN）；SB3 报告的 rollout fps（223）≠ 有效吞吐。核心训练步数据此定为 1.5M（墙钟 4.2-10.4h 恒 ≥4h）。
- 门校准初测（未温度缩放）：F4 未过——不一致率 0.359、条件误杀 0.88（门过度保守）。已加温度缩放（logit 域 Brier 搜索），待波次结束后用扩充标签（50 场景）重训重校。
- l2rpn_2019 遗留数据为旧 PypowNet 格式（_N_*.csv.bz2，加载报 ChronicsError）；get_data.py 已加格式校验并回退官方下载（下载完成，校验通过后更新本条）。
- 300k 波次完成（GNN 42/43/44 + MLP 42，旧奖励）。**初步观测：MLP 基线退化为"全 toggle"不动点**（每步 ~7 toggle 全非法被拒 → 行为=do-nothing，n_illegal≈存活步数）→ 奖励函数修订：非法动作 -1（原则性修复，对所有智能体统一）。
- **核心运行第 1 波已启动（新奖励）**：core_gnn_seed42/43/44 + core_mlp_seed42，各 1.5M 步（实测吞吐下墙钟 ≥4h），eval 每 100k 步 ×3 幕；日志 experiments/exp_002/logs/core_*.log。种子 45/46 与 MLP 43-46 待第 2 波。

## 2026-09-13 中断记录（用户重启电脑）
- 状态：核心第 1 波被中断。中断时进度（由 evaluations.npz 记录）：GNN 42≈60 万步、43≈70 万步、44≈60 万步、MLP 42≈140 万步（/150 万）。checkpoint 完整（ckpt_latest.zip 每 10 分钟）。
- 恢复方案：train_ppo 已加 --resume（SB3 加载后 learn 自动补剩余步数）；恢复命令：
  `python -m src.training.train_ppo --train-steps 1500000 --seed XX --resume experiments/exp_002/checkpoints/core_gnn_seedXX/ckpt_latest.zip --out experiments/exp_002/checkpoints/core_gnn_seedXX`（MLP 同法用 train_mlp_ppo）。
- 4h 铁律处理：运行因机器重启中断，恢复后累计训练墙钟仍 ≥4h（分段计时，train_meta.json 记录 resumed_from 与 total_timesteps；中断事实如实写入论文复现说明）。
- 重启后动作清单：①读取四状态文件（本文件/experiment_plan/decisions/outline）；②恢复 4 个 core 训练；③完成后评估+主表+exp_003/004/005；④启动第 2 波（GNN 45/46 + MLP 43-46）。

## 2026-09-13 恢复完成（重启后）
- 状态：自检通过（仓库 HEAD=7661a91，工作区干净）；4 个核心训练已用 --resume 从 ckpt_latest.zip 恢复（进程核查：3×train_ppo + 1×train_mlp_ppo 在运行）。MLP 42 剩 ~10 万步，GNN 各剩 ~80-90 万步。
- train_mlp_ppo 补齐 --resume 支持（与 train_ppo 同款）。

## 2026-09-13 断电记录（第二次中断）+ 防御性修复
- 状态：第一次恢复后约 1 小时再次断电（用户告知）。4 个 checkpoint 经 PPO.load 验证全部完好（GNN 575,541/599,042/567,297 步，MLP 1,243,137 步）。
- 诊断：断电时 stdout 块缓冲导致日志尾部丢失（首会话日志末次刷出停在 75k 表，与 checkpoint 实际进度 575k 不符）——已查明为缓冲假象，训练进度无损失（SB3 恢复计数正常）。
- 修复：所有训练改用 `python -u`（无缓冲）启动，日志实时落盘；恢复命令显式带 --eval-freq 100000（避免默认 20000 频率造成 EvalCallback 覆盖 npz 的频率不一致）。
- 二次恢复完成（-u 模式，恢复消息已实时确认）。
- 排队计划：MLP 42 完成后 → 评估 + 启动 MLP 43；随后按槽位依次 MLP 44/45/46 与 GNN 45/46（每进程 OMP=4，最多 4-5 并发）。

## 2026-09-13 核心第 1 波完成 + H4 失败（FA-002）
- 状态：4 个核心模型（GNN 42/43/44 + MLP 42，1.5M 步）训练完成；**协议评估全部等价 do-nothing**：GNN42/MLP42 全 toggle 非法、GNN43/44 永不动作。H4 失败（p≥0.05）。
- 关键证据：seed44 在 100k 步评估达 3192±1294（好策略存在过），100k→575k 间塌缩；恢复时 EvalCallback 覆盖了历史最优 best_model（已加备份防覆盖）。
- 处置：failure_analysis.md FA-002；四改进轴筛选启动（熵正则 0.01 / 课程 150k / 对手预算×2 / 丰富特征，各 1 种子 × 300k 步 × eval 50k）。
- 若筛选全部失败 → F1 退路 ①（屏蔽+IL 专家混合）或 ②（重新选题），届时按协议评估。
- 注意：第 2 波（GNN 45/46 + MLP 43-46）暂缓，待筛选胜出配置确定后按胜出配置执行。

## 2026-09-13 改进轴第一轮结果 + 第二轮启动
- 第一轮（熵/课程/强对手/丰富特征）：**全部失败**——协议评估均 1100.7±282.8、0 toggle（与 do-nothing 逐位相同）；四配置评估曲线逐位相同（694.87→602.87→940.67→701.47→415.60→467.33）= 策略从未动作。
- 关键机制发现：对手攻击断线后存在冷却期（期间重连非法 → 训练中大量非法动作的来源）；**冷却结束后的合法重连是存活任务的核心**。do-nothing 死于 ~1100 步正因从不重连。
- 第二轮筛选启动（4 并行）：①掩码动作空间 ②掩码+熵 ③丰富特征+冷却归一化边特征 ④BC 预热+RL 微调（示范正样本率仅 0.8%，BC 结果一般，pos_weight 过大导致过度预测——IL 轴预期不高，但保留为完整证据链）。
- 若第二轮仍全败 → F1 正式触发，按退路 ①（屏蔽+IL 专家混合，放弃 RL 主策略）评估；该退路的技术核心已全部具备（贪心专家 + 混合屏蔽 + 注意力解释）。

## 2026-09-13 第二轮筛选结果 + 关键机制发现
- 第二轮（掩码/掩码+熵/冷却特征/IL）：**全部失败**。掩码是唯一能产生持续动作的轴，但动作有害（评估奖励 114-236 < do-nothing）；其余轴滑回"永不动作"（评估曲线与 do-nothing 逐位相同）。
- **机制发现（决定性）**：do-nothing 死于 ~968-1100 步的元凶不是对手攻击，而是**自然负荷曲线导致的过载**（攻击预算 0/默认/×2 三配置 do-nothing 存活均为 968）。rte_case14_realistic 的生存任务中 do-nothing 近最优（L2RPN 社区共识）——单线 toggle 会降低容量，反加速高峰崩溃。
- **贪心专家 N-1 对比（2 场景）**：cid1 平均 N-1 10.08→12.87（真实提升）但存活 1381→387；cid0 首次 toggle 即猝死（存活 1）。simulate 前瞻与真实时序存在系统性差距。
- **当前**：选择实验进行中（5 种子 × 300k 步 + 25k 步编号 checkpoint，基础配置）——最后的 RL 证据。若选择实验亦失败 → F1 正式触发 → 退路①评估（安全优先框架：任务目标转向 N-1 安全度，主结果 = 零危险动作 + N-1 不恶化 + 注意力解释 + 零样本迁移；RL 生存失败作为诚实负结果写入论文）。

## 2026-09-13 五方向否决 → 方向 B 重定义立项 → 可行性尖刺通过
- 五方向 A-E 全部按协议否决（A/C/D 查重各 ≥3 篇、B 方法失败 FA-001~003、E 可行性失败），用户未作答三路径 → 按全权授权自主深挖，找到窗口内未被覆盖的具体缺口。
- **重定义题目**：电网 AI 智能体动作的**不确定性鲁棒运行时安全授权**（区间潮流最坏情况验证）。窗口内 2 篇直接相关（TwinGridShield arXiv:2608.15391、SafeVolt 10.3390/computers15070422，均经 API 核实），两者都只做标称模型验证——缺口成立。创新性声明 v2 见 paper/novelty_statement_v2.md。
- **关键机制发现**：grid2op 的 rho 是安培基准非 MW 基准（线4：35.7MW/157MW=0.23 但 rho=0.93）；DC 授权用 MW 热限，rte_case14 在 MW 意义上天然轻载——演示须用负荷增长扫描制造应力（已实现）。
- **可行性尖刺（exp_011）通过**：①遥测识别 DC 模型与 AC 潮流相关 0.9995（斜率 1.02，无需离线电网文件）；②LP 精确区间最坏情况验证 0.03s/动作；③**区间验证比标称验证多拦截 39/400=9.75% 的危险动作**（=标称验证在 ±20% 负荷失配下会误放的动作比例，论文核心量）。
- 下一步：重写 experiment_plan.md（重定义题目的完整实验计划，含铁律合规矩阵）→ 正式实验（LLM 提案源接 DeepSeek API + 基线提案源 + 失配敏感性 + 36/118-bus 迁移）。

## 2026-09-13 会话结束（用户关闭软件）——恢复点
- 无运行中的进程；git 工作区干净；所有状态已落盘。
- **恢复动作清单**（下次会话第一件事）：
  1. 启动自检（pwd/git root/顶层文件）；
  2. 读 experiment_plan.md / experiment_log.md / decisions.md / paper/outline.md；
  3. 重写 experiment_plan.md（新题目"不确定性鲁棒安全授权"的完整实验计划：大规模仿真 ≥2h 铁律档 + 消融 ≥5 维 + 跨场景 ≥3 系统 + 统计检验 + 鲁棒性 + 算力报告）；
  4. 正式实验：LLM 提案源（DeepSeek via llm-chat MCP）+ 贪心/随机基线提案源 → 标称 vs 区间授权对比 → 失配敏感性（负荷 eps 扫描）→ 36/118-bus 迁移（icaps_2021/idf_2023 dev 模式）→ exp_003 混合屏蔽器整合；
  5. 论文写作（IMRaDC + 语言门控 + 引用核实）。
- 关键文件：paper/novelty_statement_v2.md（选题声明）、failure_analysis.md FA-001~003（B 方向完整失败证据，可作为论文 Related Work 素材）、experiments/exp_011_interval_auth/results/spike_interval_auth.json（尖刺数据）。

## 2026-09-13 新题目实验启动（exp_012 主实验 + exp_017 LLM 提案）
- 实验计划 v2 已重写（铁律：大仿真 ≥2h 档；B1=TwinGridShield 式标称验证复现为 SOTA 基线）。
- **exp_017 完成（LLM 提案源，DeepSeek）**：双提示词协议——自证安全提示词下 LLM 20/20 冻结（无法自证物理安全）；验证者感知提示词下 11/20 提出动作。评估结果：LLM 提案经标称验证放行后**失配危险率 9.09%**；经区间验证放行后**恒为 0**（28.8ms/动作）。H5 成立。
- exp_012 主实验运行中（12,500 用例 × M=5000 真值 MC，≥2h 铁律档）。
- auth_eval 已参数化（--load-eps/--margin/--require-connected），消融六维就绪。
- 待办：exp_012 完成 → 分析 H1/H2 → exp_013 消融 → exp_014 鲁棒性 → exp_015 跨场景（36/118-bus）→ exp_016 算力。

## 2026-09-14 exp_012 主实验完成（H1 证实）
- **主表（11,101 用例，±20% 负荷箱，M=5000 真值 MC）**：
  - 标称验证（TwinGridShield 式）：误放率 **17.96%**（放行动作中 18% 在失配下危险；平均危险率 3.08%）；
  - 区间验证（本文）：误放率 **0.0%**（构造性保证的实证确认），误拒率仅 **1.02%**；
  - 蒙特卡洛（SafeVolt 式增强）：误放率 2.55%（统计近似的泄漏，无构造保证）。
- 累计计算时长 2.5-3h（跨会话累计；中断与续跑已如实记录）——满足 ≥2h 铁律档。
- 备注：恢复模式下延迟与收敛曲线未完整聚合——exp_016 将以专用运行补测（延迟：区间 ~29ms/标称 ~0.05ms/MC ~2ms，exp_017 已测得）；收敛曲线以专用 M 扫描运行补做。
- 下一步：exp_013 消融（13 配置）→ exp_014 鲁棒性（ε 扫描）→ exp_015 跨系统 → exp_016 算力/收敛。

## 2026-09-14 exp_013 消融 + exp_014 鲁棒性完成（H2 证实）
- **消融 13 配置：区间误放率全部为 0.0000**（构造性保证对全部消融维度稳健）。误拒率随维度合理变化：θ 界 0.3 过严（误拒 34.6%）、uniform 线路模型失配（15.2%）、遥测-30 状态与全量一致（1.9%，识别稳健）；LLM 提案误拒 0。
- **ε 扫描（真值固定 ε_ref=0.2 后的修正口径）**：区间误放率 25.61%（ε=0，=标称）→19.06%（0.05）→6.31%（0.10）→**0.00%（0.20）**→0.00%（0.40）；误拒率 0→0→0→1.9%→15.4%——单调曲线完整，ε≥真失配时构造性保证生效（理论符合）。
- **高斯量测失配**：0 误放（σ 质量基本在箱内）；**20% 负荷缺失**：0 误放、误拒 4.2%（标称恶化至 37.1%）。
- 下一步：exp_015 跨系统（icaps_2021/idf_2023 dev）→ exp_016 算力/收敛 → 论文写作。

## 2026-09-14 论文初稿完成（协议汇报节点 3）
- **main.pdf 5 页编译成功（0 错误）**：全部章节（Abstract/Intro/Methods/Results/Discussion/Conclusion）通过拼写门控；9 条参考文献全部经 API 核实；7 张图全部有生成脚本。
- 数值自查：论文所有数值与 experiments/*/results/*.json 原始记录逐条核对一致（主表 11,101 用例、消融 13 配置、ε 曲线、跨系统三系统、exp_016 延迟）。
- 诚实披露（已在论文/日志中标注）：①idf 118-bus 行为部分运行（1,104/1,500 用例，5 种子均覆盖，中断原因=会话多次退出；论文表注已声明）；②118-bus 延迟未实测（表内留空）；③语言门控用 pyspellchecker（无 Java）；④作者单位待用户补充。
- 向用户汇报初稿（见对话）。审阅意见返回后进入修订轮次（任务 6 收尾）。

## 2026-09-14 冲高升级包完成（用户反馈"工作量可提升"后）
- **带证据的拒绝**：对抗反例生成（LP argmax 即反例负荷向量）——可达性实测 257/257 全通过；论文新增"Rejection with Evidence"节（真实示例：断开线17→支路0 越限 151.5%）。
- **ε 数据驱动校准**：σ=10% 噪声、2,200 校准对 → ε=0.168/0.132/0.082（α=0.05/0.10/0.20），Hoeffding 承诺 ±0.010；论文新增校准方法节。
- **LLM 研究扩容**：20→100 状态（46/100 行动率；自证安全仍 20/20 冻结）；1150 用例评估：**未验证 60.00% / 标称 16.36% / MC 0.65% / 区间 0.00%**。
- **过程中修复两个自身 bug**（均为矛盾数字触发排查）：①"none"计数器被 decisions 循环双计（率值不变但原始计数错）；②续跑去重键缺状态身份 + LLM 提案与评估状态失配（缓存改名后重采样）。全部有修复与重跑记录。
- 论文现为 6 页、0 错误、全章节门控通过。
- 门改进链启动：标签扩至 50 场景 → 重训 → 温度缩放校准（F4 重判）。
- **门改进链完成：F4 判据未过**（50 场景标签 + 温度缩放后不一致率仍 0.36，门 AUC 0.75 为特征表达力上限）→ 按 F4 处置写 failure_analysis.md FA-001，落地**混合屏蔽器**（学习门初筛 + 精确仿真终审，误放率恒 0）。学习门削减率列为 exp_003 新判据（≥50%）。
