# 实验计划 v2（方向 B 重定义：不确定性鲁棒的运行时安全授权）

> 状态：2026-09-13 重写（选题声明见 paper/novelty_statement_v2.md；可行性尖刺 exp_011 已通过）。
> 铁律档位（AGENTS.md 2.1）：核心实验属**大规模仿真实验 ≥2h**档（动作验证与蒙特卡洛参照的总规模 ≥2h，保存中间状态与收敛曲线）；同时满足**消融 ≥5 维×3 变体**与**跨场景泛化 ≥3 个系统**两档。

## 1. 研究问题与假设

研究问题：能否构造不确定性鲁棒的安全授权层——电网 AI 智能体（LLM/启发式提案源）的拓扑动作在执行前经**区间潮流最坏情况验证**，在负荷量测与模型参数有界失配下实现**构造性零危险放行**，且保守性（误拒率）可测可校准？

| 编号 | 假设 | 可证伪判据 |
|---|---|---|
| H1（核心安全） | 区间验证放行的动作，在不确定性箱内蒙特卡洛采样（≥1000 点/动作）精确潮流下危险率=0；对照：标称验证放行的动作在失配采样下危险率>0（TwinGridShield 型脆弱性的复现与量化） | 区间组危险率>0 即机制失效（F1）；标称组危险率=0 则对照无区分力 |
| H2（保守性） | 误拒率随 ε 单调上升；ε→0 时区间验证与标称验证决策一致 | 单调性破坏或 ε=0 不一致即失败 |
| H3（校准） | 用历史遥测拟合 ε（经验分布分位数）→ 真实失配水平下误放率 ≤ 目标 α 且误拒率显著低于固定大 ε（20%） | 校准后误放率>α 或误拒率无改善即失败 |
| H4（跨系统） | 遥测识别+区间验证在 36/118-bus 同样成立（DC 相关 >0.9、误放率=0、单动作延迟 <0.5s） | 任一系统不满足即失败（F3） |
| H5（LLM 提案源） | LLM 提案源在授权层下危险动作率=0；无授权层时危险动作率>0（授权层的实际价值） | LLM 无法生成合法动作（F4）或两类危险率无差异 |

## 2. 变量

- **自变量**：验证器类型（无/标称/区间/蒙特卡洛）；不确定性宽度 ε∈{0,0.05,0.1,0.2,0.4}；提案源（LLM/贪心/随机/规则）；安全裕度 margin∈{0,0.02,0.05}；DC 模型来源（遥测识别/离线文件/均匀电抗）；负荷增长系数 λ∈{1,2,3,4,5}（应力扫描）。
- **因变量**：危险放行率（失配采样下）、误拒率、单动作延迟、DC-AC 保真度（相关/斜率）、MC 收敛曲线。
- **控制变量**：场景/状态采样随机种子 {0..4}；候选动作生成规则；热限值（env.get_thermal_limit）；LP 求解器 highs；箱对称性 ±ε。

## 3. 对照组（铁律：≥3 基线，含 2024-2026 SOTA）

| 基线 | 说明 | 用途 |
|---|---|---|
| B0 无验证 | 提案动作直接执行 | 危险率上界参照 |
| B1 标称验证 | 单一标称模型的确定性验证——**TwinGridShield（arXiv:2608.15391, 2026）机制的直接复现**，窗口内 SOTA 基线 | 失配脆弱性的量化对象 |
| B2 区间验证（本文） | LP 精确最坏情况验证 | 主方法 |
| B3 蒙特卡洛验证 | 每动作采样 100 点精确潮流取经验最坏情况（SafeVolt 式 simulator 评估的增强版；统计近似、无构造保证） | 概率参照与计算成本对照 |

## 4. 实验矩阵

| exp_id | 名称 | 铁律档位 | 种子 | 预估时长 |
|---|---|---|---|---|
| exp_012 | 主实验：B0-B3 × 4 提案源 × 20 状态 × 5 候选 × 5 个 λ 档（动作验证 ≥10^4 次 + MC 参照 ≥10^6 次精确潮流） | 大仿真 ≥2h | 5 | 2-4h |
| exp_013 | 消融 6 维×3 变体（ε/验证量/DC来源/提案源/margin/判据） | 消融 ≥5 维 | 5 | 1-2h |
| exp_014 | 鲁棒性：失配水平扫描（ε 0→40%）、负荷量测高斯噪声、20% 负荷缺失（箱加宽）→ 危险率曲线 | 鲁棒性 | 5 | ~1h |
| exp_015 | 跨场景：14/36/118-bus 独立完整流程（遥测识别→授权对比） | 跨场景 ≥3 系统 | 5/系统 | ~1h |
| exp_016 | 计算成本：单动作延迟（4 验证器）、LP 规模敏感性、与 AC 精确潮流对照 | 成本报告 | — | ~0.5h |
| exp_017 | LLM 提案源：DeepSeek 提示词协议 → 提案数据集 → 有/无授权层危险率对比 | — | 5 | 提案生成 ~1h |

## 5. 预期结果

- H1：区间组危险率=0（1000 点 MC × 全部放行动作）；标称组危险率 ≈ 5-15%（与 exp_011 尖刺的 9.75% 量级一致）。
- H2：误拒率 ε 单调；ε=0 与标称一致（决策矩阵逐位相同）。
- H3：校准 ε ≈ 观测误差分位数；误放率 ≤5% 且误拒率比固定 20% 降低 ≥30%。
- H4：36/118-bus DC 相关 >0.9，延迟 <0.5s（118-bus 的 LP 为 117 变量，highs 毫秒级）。
- H5：LLM 提案的无授权危险率 3-10%；授权后 =0。

## 6. 失败判据

- **F1**：区间验证的 MC 确认出现危险放行 → 停手，检查 LP 实现与箱定义（构造性保证的实证违反只能是实现错误）。
- **F2**：校准后误拒率 >50% → 保守性不可接受，改更紧参数化（仿射算术/椭球不确定性集）。
- **F3**：36/118-bus 遥测识别失败（相关 <0.9）或延迟 >0.5s → 检查大系统 LP 与识别采样数。
- **F4**：LLM 提案源无法生成合法动作 → 提案协议改进 2 轮；仍失败则降级为合成提案源并在论文如实标记。
- **F5**：同一实验连续失败 3 次 → 停止，输出完整错误堆栈与推理过程到 failure_analysis.md。

## 7. 铁律合规自查

| 条款 | 落点 |
|---|---|
| 大仿真 ≥2h + 中间状态/收敛曲线 | exp_012（≥10^4 动作验证 + ≥10^6 MC 精确潮流）；MC 收敛曲线（危险率 vs 样本数）落盘 |
| ≥3 基线（含 2024-2026 SOTA） | B0/B1(TwinGridShield 式复现)/B2/B3 |
| ≥5 种子 + 均值±标准差 + 配对检验 p 值 | 场景级配对检验口径（5 种子 × 场景，decisions.md 已定）；危险率显著性用置换检验 |
| 消融 ≥5 维×3 变体 | exp_013 六维 |
| 噪声/缺失/分布偏移退化曲线 | exp_014 |
| 训练/推理时间、参数量、内存 | exp_016（本题目无训练模型；报告 LP 延迟与规模） |
| 跨场景 ≥3 系统独立完整流程 | exp_015 |
| 实验计划/日志/失败分析文件 | 本文件 / experiment_log.md / failure_analysis.md |
| 每图有生成脚本 | 各 exp 的 results/ 下 |
| 固定种子并报告 | 种子 {0..4} 写入论文 |

## 8. 执行细则

- 环境：conda gridgnn；运行前 `source setenv.sh`；训练类脚本用 `python -u`。
- LLM 提案源：DeepSeek（llm-chat MCP 由执行代理调用），提示词协议固定（系统提示 + 状态摘要 + 动作模板），提案保存为数据集（data/processed/llm_proposals_*.json）供评估脚本消费；论文报告提示词全文与模型版本，reproduce.sh 在无 API 密钥时回退到随库提案数据集。
- 每阶段结束追加 experiment_log.md；数值只允许引用各 exp 的 JSON/CSV 落盘记录。
- 冒烟测试：每个新评估脚本先跑 --smoke 小规模。

## 9. 顺序与门控

exp_012（冒烟→全量）→ 若 F1 → failure_analysis.md → 修复 → 重跑；H1 通过后 → exp_013/014/015/016 → exp_017（LLM 提案）→ 实验门控自检 → 论文写作（任务 6）。
