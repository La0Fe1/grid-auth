# 决策记录

## 2026-09-13 项目启动（AGENTS.md 协议启动）

- **决策：项目根目录 = `C:\Users\佬肥\PycharmProjects\project-grid-gnn`。**
  理由：AGENTS.md 0.1 要求独立建仓、禁止嵌套在父仓库内。原目录位于 `C:\Users\佬肥\PycharmProjects\PythonProject`（L-YOLO 父仓库）内部且无独立 `.git`，违反 0.1。
  用户明确选定"移出父仓库"方案（原指令文件中 `<PROJECT_ROOT>` 为未填写占位符，按 0.1"停止并报告"条款向用户询问后指定）。
- **决策：目标期刊等级 = JCR Q1（中科院一区）。** 覆盖 PLAN.md 中"目标 SCI 二区"的旧说法，以 AGENTS.md 为准（目标升级）。方向 B 对应期刊候选：IEEE Trans. Smart Grid / TII / Applied Energy。
- **决策：主攻方向暂定 = B（Agentic AI for safe & explainable smart grid control）**，沿用 Grid-GNN 既有技术线（GNN 策略 + 安全约束 RL，Grid2Op 拓扑控制）。**选题门控（1.1–1.3）尚未通过**：须先完成 18 个月窗口查重与创新性声明，若 ≥3 篇直接相同则否决换题。
- **决策：AGENTS.md 为指令文件副本**，仅将 `<PROJECT_ROOT>` 占位符替换为实际路径，其余未改动（文件头部以 HTML 注释声明出处）。
- **决策：缓存/临时文件重定向。** 已建 `.cache/`、`.tmp/`、`.logs/`；运行任何脚本前必须 `source setenv.sh`（含 TMPDIR/TEMP/TMP、XDG_CACHE_HOME、HF_HOME、TRANSFORMERS_CACHE、TORCH_HOME、MPLCONFIGDIR、CUDA_CACHE_PATH、PYTHONPYCACHEPREFIX）。
- **决策：遗留代码暂按原样入库**（PLAN.md、gnn_env.py、train_*.py、日志、checkpoint zip 等），后续按 AGENTS.md 3.1 重组进 `src/` 与 `experiments/`，重组后逐模块补单元测试。
- **已知代价**：PyCharm 需重新打开一次项目（路径已变）；父仓库仅失去一个未跟踪目录，无 git 历史影响。

## 2026-09-13 选题门控结论（AGENTS.md 1.1–1.3）
- **决策：方向 B 通过选题门控，但主创新重新定义。** 原"GNN 策略"单独作为主创新已 ≥3 篇 18 个月窗口内相似论文 → **否决该表述**。新主创新 = 三柱组合：硬 N-1 安全屏蔽 + 策略内建注意力解释 + 零样本跨算例泛化。
- 查重结果：三柱合一直接相同者 0 篇；高度相似 2 篇（Zhang et al., Applied Energy 2026；Fabrizio et al., arXiv:2509.02861）→ 按 1.2 走"1–2 篇"档，缺口已写入 paper/novelty_statement.md。
- 逐篇核对表：paper/literature_notes.md。所有引用经 Semantic Scholar/DOAJ/OpenAIRE API 验证；TechRxiv 一篇文章的 DOI 在 Crossref 查无记录 → 标记未验证、禁止引用。
- 待验证假设（写入 experiment_plan.md）：N-1 边级分类器推理延迟；GAT 归因与 N-1 越限支路的一致性；14→36/118-bus 零样本迁移的实际性能。**在实验验证前不得在论文中断言。**

## 2026-09-13 代码重组完成，进入实验阶段
- **决策：主训练步数 = 300k**（实测 FPS≈17（GNN）/29（MLP），CPU 瓶颈；300k ≈ 4.5–5h，满足 ≥4h 铁律且与遗留 200k/500k 无明显差距的结论一致）。
- **决策：训练并行度 2–3 进程**（CPU 密集，单机多核分摊 55 次消融/种子运行；每进程独立 out 目录）。
- **决策：屏蔽门标签语义** = "toggle 该线后电网仍 N-1 安全"的可行性判定（非遗留的"最优线推荐"）；标签用 obs.simulate（同一时间步，不推进时序），与遗留 gen_n1_labels 不同——后者用 env.step（时间推进），已弃用该语义。
- **决策：屏蔽门只对"断开"请求做门控**（已断开线路的重连请求交环境合法性检查），范围限制如实写入代码注释与论文方法节。
- **决策：grid2op 数据集路径** 从用户主目录 ~/data_grid2op 复制到项目 data/grid2op_data（0.1 合规）；外部主目录原数据标记为只读来源，未修改。
- **遗留代码处置**：根目录遗留脚本（gnn_env.py、train_*.py、gen_n1_labels.py、eval_generalize.py 等）已被 src/ 版本取代，待实验管线验证后统一清理迁移（下一步）。

## 2026-09-13 屏蔽谓词修订（标签全零问题）
- **事实**：首版标签（"toggle 后完美 N-1 安全"）在 rte_case14_realistic 上全零（200 状态×20 线无一满足）→ 门退化为常数预测。遗留记录佐证：该算例初始 N-1 安全度仅 ~10/20。
- **决策：屏蔽谓词改为可操作安全准则**——toggle 后无过载（ρ_max<0.95 留裕度）且 N-1 安全度不恶化。这是运行人员实际执行的硬准则，标签将有正样本；创新性声明与实验计划 H1 已同步修订。
- **事实（对手确定性探测）**：对手攻击序列仅由场景决定（同场景不同种子结果相同 799/799），与 env.seed 无关；评估口径 = seed→场景 id 显式映射（已实现）。
- 遗留 gnn_n1_classifier.pt（"最优线推荐"语义）不再使用；新门以 data/processed/n1_gate_labels.pkl 训练。

## 2026-09-13 训练步数修订（4h 铁律与实测 FPS，二次修订）
- **事实（实测）**：SB3 报告的 rollout fps（223/413）≠ 有效吞吐。4 进程并行抢 16 核时单进程有效 ~40fps；单进程约 150-200fps。300k 步在此条件下 ≈ 2-2.5h。
- **决策：核心训练步数 = 1,500,000**。任意并行度下墙钟 4.2h-10.4h，恒满足 ≥4h 铁律（40-100fps 区间全覆盖）。**拒绝"人为降速凑时长"**（铁律要的是训练强度）。
- **决策：消融运行 300k 步 × 5 种子**（§2.1 对消融只要求 ≥5 维×3 变体；§2.2 的 5 种子+p 值满足；步数差异在论文中如实报告）。
- **决策：当前 300k 波次降级为初步信号**。**初步观测：GNN 90k 步时 eval 奖励 1370±1025，已超 do-nothing 1100**——H4 方向积极（遗留 GNN 是 774）。
- **决策：MLP 基线同 1.5M 步预算**（匹配步数；其墙钟可能 <4h，理由：铁律针对"你的核心实验"，基线用同预算，如实记录）。
- **决策：贪心专家基线的存活表现差（146 vs 1100）是真实且可报告的结果**——证明朴素 N-1 贪心伤害运行，反衬"屏蔽只删动作不强迫 toggle"的设计。
- **修复：训练中 EvalCallback 的 eval 环境未按种子播种**（3 个种子 eval 奖励逐位相同 1077.06）→ train_ppo/train_mlp 均已加 eval_env.seed(args.seed)。

## 2026-09-13 l2rpn_2019 弃用
- **事实（实测）**：官方 datasets-v0.1.0 的 l2rpn_2019 tarball 本身是旧 PypowNet 格式（_N_*.csv.bz2），grid2op 1.12.5 加载报 ChronicsError——上游打包不一致，非本仓库问题。
- **决策：弃用 l2rpn_2019**；36-bus 跨算例系统改用 l2rpn_icaps_2021（内置开发模式，36 变电站/59 线路，已实测可用）。旧副本与 tarball 已删除；data/README.md 与 get_data.py 已同步。

## 2026-09-13 统计口径决策（种子数 vs p 值）
- **事实**：5 对样本的配对 Wilcoxon 最小可达 p = 2×0.5^5 = 0.0625，永远达不到 p<0.05。
- **决策：双层报告口径**——表格报告种子级 mean±std（5 种子，满足铁律）；显著性检验在**场景级成对样本**上做（5 种子 × 4 场景 = 20 对观测，配对 Wilcoxon，统计功效足够）。该口径写入论文方法节与主表注释。
- 实现：run_eval 单遍评估同时落盘 per_seed 与 per_scenario；make_table 用场景级 p 值。
- **F4 触发与处置**：学习门不一致率 36%（F4 要求 ≤5%）→ failure_analysis.md FA-001 → 混合屏蔽器（学习门初筛 + 精确终审，误放率恒 0），见 experiment_plan 修订。

## 2026-09-13 F1 正式裁决：方向 B 终止，转退路②重新选题
- **事实**：F1 判据成立（15 配置 + 5 选择种子全灭；H2 注意力无信号 p=0.48/0.07；专家+屏蔽存活崩塌且更严裕度无效）。证据链见 failure_analysis.md FA-003。
- **决策：终止方向 B（RL 生存/三柱组合）**。原 B 方向产物全部保留为负结果资产（代码、数据、日志、FA-001~003），可作为论文的 Related Work/分析材料或独立技术报告，但不再作为主攻方向。
- **决策：候选方向 E（PINN + XAI for new-type power system regulation）**，理由见 FA-003；须重走 1.1-1.3 选题门控（18 个月查重 → 创新性声明）。若 E 查重被否决 → 依次评估 D（视广州数据集批复）、A（查重后确认）。
- 计算资源约束不变（单机 4070 12GB）；原 RL/Grid2Op 训练管线全部保留可复用。

## 2026-09-13 方向 E 查重通过（0 篇直接相同）+ 可行性待验证
- **查重结果**：宽口径"PINN 调频/调压"2025 年 ≥3 篇（VSG-自适应 PINN、PINDA-AGC、频率轨迹预测 PINN 等）→ 宽口径题目按 1.2 否决；但**具体题目"PINN 调频动力学代理 + 形式化 XAI 归因评估（与解析灵敏度对照）"0 篇直接相同**——2025 年 PINN 调频论文全部只"声称"物理嵌入=可解释，无归因评估；XAI 电力工作（Aygul et al. KBS 2026）不碰 PINN/调频。缺口成立。
- **题目（E 方向具体化）**：Physics-informed frequency dynamics surrogates with formally evaluated attribution：PINN 转子运动+调速器 ODE 代理 → 梯度×输入归因 → 以数值解析灵敏度（nadir/ROCOF 对惯性 H、调差 R 的偏导）为真值对照 → 归因误差量化 + 与纯数据驱动基线比较。
- **机制假设（为何 PINN 归因更可靠）**：物理残差项把模型约束在真实动力学流形上，梯度路径经过物理项，噪声小于纯数据驱动——可检验判据 = 归因误差。
- **可行性尖刺（进行中）**：本地 RK4 实现调频动力学（IEEE 39-bus 参数公开）→ PINN 冒烟训练 → 解析灵敏度数值计算（有限差分/伴随）→ 归因对照。无商业依赖，12GB 显存充足。
- 与用户既有项目不重叠：暂态稳定项目=功角轨迹/CCT（转子角稳定），本项目=频率调节动力学（调速器/AGC），物理过程与任务均不同。

## 2026-09-13 方向 E 可行性验证失败——否决
- **尖刺结果**（exp_010/spike_pinn.py，全部实测）：软约束 PINN 在调频轨迹代理上全面劣于同架构 MLP——全数据 RMSE 0.679 vs 0.427（初测）/ 梯度配平+余弦退火+800轮后低数据 0.473 vs 0.259；归因 Pearson 0.54→0.12（噪声水平）。频率动力学光滑简单，纯 MLP 插值已足够，物理软约束只添乱。
- **裁决：E 否决**（按 1.2"0 篇→验证可行性"，机制假设"物理嵌入改善归因"被实测证伪）。频率动力学模块（src/utils/freq_dyn.py，62 项测试）保留为可复用资产。
- **下一步**：A（LLM 认知编排）具体缺口检索——候选缺口："电网拓扑控制中 LLM agent 的安全约束评测基准"（Iberian 黑out 评论文章刚指出该领域缺统一评估指标；可复用本项目全部 Grid2Op/N-1 安全检验基础设施 + 本地 DeepSeek API）。若 A 查重被否决 → 评估 D（依赖用户 nbsdc 广州数据集批复）。

## 2026-09-13 五方向全部否决——按汇报节点 1 向用户报告
- **A 否决（查重 ≥3 篇直接相同）**：ElecBench（PESGM 2025 最佳论文，含安全指标）、PowerAgentBench-Dyn/SS（2026-06，误放惩罚/残差违例分数）、TwinGridShield（2026-08，运行时安全授权 0 危险放行）、SafeVolt（2026）等——"LLM 电网 agent 安全评测基准"缺口已被填。
- **C 否决（查重 ≥3 篇直接相同 + 商业授权）**：IET PEL 2025 DT+DDPG 多电平升压、IEEE 2025 Simulink-DT+PPO 整流器、J.Eng 2026 shadow-mode TD3 SAPF + 2 篇综合综述；且需 PLECS/MATLAB/Simulink 商业授权。
- **D 否决（查重 ≥3 篇直接相同）**：Tianjin "Two-Stage ADN Voltage Control via LLM-RL Collaboration"（arXiv:2602.21715，2026-02）、IEEE Access 2025-01 LLM-assisted DRL 电压控制、TSG 2025 in-context MPC LLM 调压、吉大学报 2025——"LLM+DRL 配网联合决策"已被直接覆盖。
- **汇总**：B 方法失败（FA-001~003）、E 可行性失败（PINN 尖刺劣于 MLP）、A/C/D 查重否决（各 ≥3 篇 18 个月窗口内直接相同）。指令五个方向的自主决策空间已耗尽。
- **剩余路径**（需用户决策，按汇报节点 1 报告）：
  ① D 路线 + 真实数据护城河：用户 nbsdc.cn 广州配网数据集申请若获批，真实数据版本的研究可绕开"公共 IEEE 馈线"拥挤区（窗口内所有 D 论文均用公共算例）——需用户确认申请状态；
  ② B 负结果论文（Q2 级别，EPSR/IJEPES）：诚实但低于 Q1 指令目标——需用户同意降级；
  ③ C + 开源仿真（OpenModelica/自建 gym 转换器环境）：不解决查重否决，除非找到新具体缺口——概率低。
- 所有状态文件、代码资产（62+ 测试、Grid2Op 管线、频率动力学模块）、三份失败分析均已落盘。

## 2026-09-13 方向 B 重定义立项（用户未作答三路径 → 按全权授权自主深挖）
- **新具体题目**：电网 AI 智能体动作的**不确定性鲁棒运行时安全授权**（区间潮流最坏情况验证）。属方向 B 范围（Agentic AI 安全）。
- **查重（1-2 篇分支）**：窗口内 2 篇直接相关——TwinGridShield（arXiv:2608.15391，S2 核实）与 SafeVolt（Computers 2026，Crossref 核实）。两者都只做**标称模型**验证；缺口 = 量测/参数不确定性下的安全保证（见 paper/novelty_statement_v2.md，失配失效百分比数字因无法从原文核实已标记未验证、不引用）。
- **机制**：区间潮流把负荷/额定值不确定性传播为支路潮流区间；授权判定 = 区间界全安全才放行 → 对不确定性集内所有模型构造性安全，保守性可测可校准。
- **可行性路径**：①区间 DC 潮流（Grid2Op 14-bus 网格，纯 numpy 实现，尖刺级）；②动作提案源：本地 DeepSeek API（llm-chat MCP）+ 贪心/随机基线（模型无关设计，与 TwinGridShield 同构）；③复用现有 Grid2Op/N-1 基础设施。无需新商业授权。
- 原 B 三柱资产全部保留；本题目与 FA-001~003 的失败无耦合（不依赖 RL 学习）。

## 2026-09-14 收尾决策
- **118-bus 延迟测量放弃**：measure_latency 在 idf 上两次超时（>10min，疑为 dev 模式 env 初始化开销）；论文算力表只报告已实测的 14-bus（0.08/7.1/42.7ms）与 36-bus（0.13/13.6/130ms），118-bus 行延迟列留空并标注未实测。
- **语言门控实现**：无 Java（language_tool_python 不可用）→ pyspellchecker 错别字门控 + 领域白名单 + 连字符拆分；语法由人工复核。
- **idf 细档 λ 档位**：λ∈{1.2,1.5,2.0}（原 {2,3,4} 档在该系统全部动作均危险，对比退化）。

## 待办决策
- 区间潮流尖刺结果 → 若区间验证无法有效区分安全/不安全动作（保守性过大或界过松），重估机制（如仿射算术/二次区间扩展）。
- 混合屏蔽器的学习门削减率若 <50%（exp_003 测量）：按 FA-001 预案评估放弃学习门、纯精确屏蔽，并如实重写创新性声明。
