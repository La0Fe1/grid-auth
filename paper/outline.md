# 论文大纲与写作进度（方向 B 重定义：不确定性鲁棒运行时安全授权）

> 结构要求（AGENTS.md 4.1，IMRaDC）。每章完成后立即执行写作门控：
> language_tool_python 逐段扫描（同段 ≥2 警告→整段重写）、glossary.md 符号一致性、
> 引用 DOI 验证、图表 caption 自包含且坐标轴带单位。

## 拟定题目
Uncertainty-Robust Runtime Authorization for Grid AI Agent Actions: Worst-Case Verification via Interval Power Flow

## 目标期刊（方向 B 系统级安全应用）
首选 IEEE Trans. Smart Grid；备选 Applied Energy / TII。参考文献 ≤30，自引 ≤2。

## 章节大纲与进度

### 1. Introduction（未写）
- 漏斗：LLM/智能体进入电网运行决策（TwinGridShield、SafeVolt 的部署语境）→ 运行时安全授权的兴起 → **缺口：标称孪生验证对量测/参数失配零防护**（TwinGridShield 的确定性孪生假设；SafeVolt 的标称仿真器）→ 本文贡献（1.区间最坏情况授权：LP 精确区间潮流，箱内全模型构造性安全；2.遥测识别 DC 模型，无需离线电网文件；3.LLM 提案行为研究：自证安全=冻结 20/20 vs 验证者感知=行动 11/20，授权层消除两难；4.保守性校准与跨系统验证）。
- 贡献表述遵循学术谦抑原则（不用"首次/首创"）。

### 2. Methods（未写）
- 2.1 问题定义：DC 电网模型、动作（线路断开集合）、安全谓词（|f| ≤ (1−m)×rating）、不确定性箱（负荷 ±ε）。
- 2.2 遥测网络识别：x_ℓ = LS(θ_or−θ_ex, p_or)（保真度实测 r=0.9995）。
- 2.3 区间最坏情况验证：线性性 → LP 精确区间（含 θ 界扩展、连通性前置）；构造性安全引理 + 证明草图；算法伪代码。
- 2.4 基线验证器：无验证 / 标称（TwinGridShield 式）/ 蒙特卡洛（SafeVolt 式增强）。
- 2.5 LLM 提案协议：双提示词变体全文。
- 2.6 评估协议与统计：场景级配对检验口径、误放率/误拒率定义、MC 真值收敛。

### 3. Results（部分数据已得）
- 3.1 主对比（exp_012 运行中）：标称 vs 区间 vs MC 的误放/误拒/延迟；真值 MC 收敛曲线。
- 3.2 LLM 提案研究（exp_017 ✅）：冻结/行动双行为；标称放行 9.09% 失配危险 vs 区间 0%。
- 3.3 消融（exp_013 待跑）：ε/验证量/线路模型/提案源/margin/判据。
- 3.4 鲁棒性（exp_014 待跑）：失配扫描、噪声、缺失。
- 3.5 跨系统（exp_015 待跑）：36/118-bus。
- 3.6 计算成本（exp_016 待跑）：28.8ms/动作（区间）等。

### 4. Discussion（未写）
- 保守性-安全性权衡与 ε 校准；箱不确定性的局限（vs 椭球）；DC 近似的适用边界（实测相关 0.9995）；
- 与 TwinGridShield（标称）与 SafeVolt（标称仿真器+裁判）的定性对比；部署含义（冻结 vs 危险的两难解除）。
- 局限性：静态单步动作、负荷箱对称性、无 AC 校验层（可作 future work）。

### 5. Conclusion（未写，≤300 字）

## 图表清单（每图需生成脚本）
- F1 框架图（提案源→授权层→执行；图内标注区间验证）
- F2 遥测识别散点（DC vs AC，r=0.9995）——exp_011 数据
- F3 主对比柱状/曲线（误放率 4 验证器 × λ）——exp_012
- F4 MC 收敛曲线（危险率 vs M）——exp_012
- F5 LLM 行为对比（冻结 vs 行动 + 授权前后危险率）——exp_017
- F6 消融矩阵热图——exp_013
- F7 失配扫描曲线——exp_014
- T1 主表（验证器 × 误放/误拒/延迟 + p 值）
- T2 LLM 提案表；T3 消融表；T4 跨系统表；T5 算力表

## 待办
- [ ] exp_012/013/014/015/016 完成并落盘
- [ ] glossary.md 术语表
- [ ] references.bib（全部 DOI 验证）
- [ ] 各章节写作 + 门控
