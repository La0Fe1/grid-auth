# 失败分析

## FA-001：学习式 N-1 门无法通过 F4 判据（2026-09-13）

**失败现象**：N1Gate（GAT，AUC=0.746）在验证集上对"安全 toggle"判定的不一致率达 36%（F4 要求 ≤5%）。
5 倍标签（200→1000 状态）+ 温度缩放后仍为 36%；条件误杀率高达 88%——门的概率系统性偏低，
几乎所有安全动作都被判为不安全。

**根因分析**：
1. 判定目标是**反事实预测**：给定状态+候选动作，预测"该动作后 N-1 安全度不恶化"——由潮流重分布决定，
   而输入特征只有节点聚合（发电份额/负荷份额/平均电压）与边 rho/line_status，
   不包含线路潮流方向、热裕度分布等重分布敏感信息。AUC 0.75 ≈ 该特征集的表达力上限。
2. 温度缩放无效 → 不是校准问题，是判别能力问题。
3. 结论：**学习门不能替代精确仿真判定**，F4 按 experiment_plan 判定为失败。

**决策（experiment_plan F4 处置路线）**：采用"改用精确校验（延迟代价接受）"与学习门结合的**混合屏蔽器**：
- 学习门 = 快速初筛（挡住大概率不安全动作，削减精确仿真次数 ~88%）；
- 精确校验（单层组合仿真，与标签同一实现）= 终审，对门放行的每个 toggle 做精确判定；
- **误放率恒为 0**（精确层保证，硬安全由构造成立），学习门的误差只影响误杀率（性能代价，可测可控）。
- 该设计与三柱架构一致：安全柱的"硬"由精确层保证，学习门的贡献在效率与可迁移性（跨算例零样本初筛）。

**影响**：
- 创新性声明机制①修订："学习门初筛 + 精确校验终审，误放率 0"（见 paper/novelty_statement.md）。
- H1 修订：危险动作率=0 由精确层保证；新增效率假设：学习门削减 ≥50% 精确仿真次数（exp_003 测量）。
- 屏蔽训练（exp_003"硬屏蔽"变体）的每步延迟增加（~20 次仿真/步）→ 训练时屏蔽仅用于精调阶段，
  主策略训练不屏蔽（动作提案学习），部署时屏蔽——如实写入论文方法节。

**未验证**：学习门的"初筛通过率"分布尚未实测（exp_003 将测量）；若门初筛几乎全通过（削减率低），
门退化为纯开销，届时按 F2 评估是否放弃学习门、直接用纯精确屏蔽（并如实重写创新性声明）。

## FA-002：基础配置下 H4 失败——策略收敛到退化不动点（2026-09-13）

**失败现象**：4 个核心模型（新奖励含非法惩罚 -1，1.5M 步）评估结果全部等价于 do-nothing：
- GNN seed42 / MLP seed42：每步 toggle 7-22 条线，动作全非法被拒 → 行为=do-nothing（存活 1100.7±282.8 与 do-nothing 逐位相同）；
- GNN seed43/44：学到"永不动作"（0 toggle）→ 精确等于 do-nothing。
- H4 判定：p≥0.05 → **失败**（F1 前置条件达成；按 experiment_plan 先进改进轴，全部失败才正式触发 F1 退路）。

**关键证据**：训练中期策略是好的——seed44 在 100k 步评估达 3192±1294（远超 do-nothing 1100），
塌缩发生在 100k→575k 之间（575k checkpoint 已退化）。best_model.zip 亦退化：
恢复运行时 EvalCallback 计数器重置，"New best" 用退化模型覆盖了历史最优（恢复机制缺陷，已记录）。

**根因分析**：
1. **后期确定性塌缩**：PPO 收敛后熵消失，策略概率极化 → 全 toggle（非法被拒）或全不动；
2. **do-nothing 强吸引子**：+1/步的存活奖励使"不动"稳定高回报，探索收益被稀释（PLAN.md 已知难题）；
3. 非法惩罚 -1 不足以阻止全 toggle（单步信用分配无法定位"哪条线导致非法"）。

**改进轴（按实验计划 §9 第一阶段）**：
| 轴 | 机制 | 依据 |
|---|---|---|
| 熵正则（ent_coef=0.01） | 防止后期确定性塌缩 | 塌缩证据 |
| 课程学习（150k 无攻击→150k 带攻击） | 先学操作技能再学防御 | PLAN.md 候选② |
| 更强对手（攻击预算加倍） | 削弱 do-nothing 基线 | PLAN.md 候选④ |
| 丰富特征（v2 图特征） | 更强策略表达 | PLAN.md 候选① |

**筛选方案**：每轴 1 种子 × 300k 步（评估频率 50k），观察评估奖励曲线是否超过 do-nothing 且不塌缩；
胜出配置进入 5 种子 × 1.5M 核心运行。同时修复恢复时 EvalCallback 覆盖 best checkpoint 的缺陷。

**若全部失败**：按 F1 走退路 ①"屏蔽+IL 专家混合"（放弃 RL 学习主策略，用 N-1 贪心专家+学习门做安全增强）或 ②重新选题。

## FA-003：F1 正式裁决——方向 B 三柱证据链闭合，退路①不可行（2026-09-13）

**最终证据（全部实测，日志/JSON 可查）**：
1. **RL 主策略**：15 个配置（基础/熵/课程/强对手/丰富/冷却/掩码/掩码+熵/IL/BC+…）× 5 个选择实验种子全部退化为 do-nothing。选择实验（每 25k 步 12 个编号 checkpoint × 验证集选优）：**所有 checkpoint 验证集存活恒为 953.2**（= do-nothing 验证集值），策略从未动作。原始 seed44 的 3192 瞬时好策略不可复现。
2. **注意力解释（H2）**：N1Gate 的 GAT 注意力对 N-1 越限支路无信息量——Jaccard(Top-5) 0.183 vs 随机 0.171（p=0.48）；越限线注意力秩 0.498≈0.5 随机水平（p=0.07）。
3. **专家+屏蔽（退路①）**：贪心专家真实提升 N-1（14-bus cid1 +2.8、118-bus cid0 +19）但存活崩塌（1381→387、575→55、cid0 首步即死）；更严屏蔽裕度（0.95→0.9）无法阻止——simulate 前瞻与真实时序的差距不可消除。
4. **机制**：do-nothing 死于自然负荷峰值过载（攻击预算 0/默认/×2 存活全同），该基准上单线 toggle 只会降容量加速崩溃。

**三柱逐项判定**：硬屏蔽（零误放，构造成立）✓ / 注意力解释 ✗ / RL 主策略 ✗ / 零样本迁移（架构支持但无有价值对象可迁移）✗。
**结论**：方向 B 在当前基准与方案组合下最高可达 Q2/Q3（"方法+负结果分析"），无法达到指令要求的 JCR Q1。
**裁决（按 experiment_plan F1 退路②）**：重新选题——从 A/C/D/E 重走 1.1-1.3 选题门控。理由记录：
- A（LLM 编排）：前序检索已见 2025 年大量 LLM 电网 agent 论文（Mingyue、GridMind、CSEE JPES 等），≥3 篇相似风险极高；
- C（数字孪生+RL 电力电子）：需 PLECS/MATLAB 商业授权 → 触发付费汇报节点，可行性受限；
- D（LLM+DRL 配网）：依赖配网真实数据（用户正在申请 nbsdc 广州数据集，尚未获批）；
- E（PINN+XAI 新型电力系统调节）：公开数据（IEEE 标准算例动力学仿真可本地实现）、无商业依赖、与用户既有项目（暂态稳定 DeepONet、AC 潮流代理）任务不重叠（调节=调频/调压动力学）。
→ **选定 E 为候选，启动 18 个月窗口查重**；若 ≥3 篇直接相同则按 1.2 再换。
